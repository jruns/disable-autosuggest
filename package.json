{
    "scripts": {
        "build": "web-ext build --source-dir ./src/ --ignore-files _metadata* --filename disable-autosuggest.zip --overwrite-dest",
        "sign": "web-ext sign --source-dir ./src/ --ignore-files _metadata* ",
        "start:firefox": "web-ext run --source-dir ./src/"
    }
}